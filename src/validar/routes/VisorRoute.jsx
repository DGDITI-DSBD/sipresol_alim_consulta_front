import React from 'react'
import { Navigate, Route, Routes } from 'react-router-dom';
import CryptoJS from 'crypto-js';
import { Visor } from '../pages';
import { pass } from '../../api';

export const VisorRoute = () => {
    return (
        <div>
            <Routes>
                
            </Routes>
        </div>
    )
};
